Dependencies:
1. xmobar is recompiled and installed (by stack) as part of this package.
   Thus, i have two additional (system) dependencies:
    - xpm.h (libxpm-dev on Debian).
    - iwlib library (libiw-dev on Debian).
